query GetCasesByNameIds($ids: [ID]) {
  cases(
    filters: { case_names: { id: { in: $ids } } }
    pagination: { limit: 150 }
  ) {
    data {
      id
      attributes {
        ...CaseFragment
      }
    }
  }
}
